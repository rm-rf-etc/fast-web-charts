<style>
  :host {
    width: 100%;
    height: 100%;
    box-shadow: 0px 5px 5px var(--black1);
  }
  #header {
    position: relative;
    width: 100%;
    height: 100%;
    color: var(--text-primary);
    background-color: var(--black2);
    border-bottom: 1px solid var(--border1);
  }
  ::slotted(*) {
    position: absolute;
    top: 50%;
    left: 0;
    transform: translateY(-50%);
    grid-row: 1;
    grid-column: 1 / 3;
    padding-left: 5px;
    margin: 0;
    box-sizing: border-box;
  }
</style>
<div id="header">
  <slot></slot>
</div>
