import { paginatedDataClient } from './paginated-data-client';

describe('paginatedDataClient', () => {
  it('should work', () => {
    expect(paginatedDataClient()).toEqual('paginated-data-client');
  });
});
